# Legalize Format Spec v0.4 — https://github.com/legalize-dev/legalize/blob/main/SPEC.md
#
# How to build the path to any law in this repo. A placeholder is either a value
# the spec derives (directory, id_sha1_2, identifier) or a key of the law's own
# frontmatter, used verbatim.
# Generated by the pipeline; do not edit by hand.

spec_version: "0.4"
country: "pt"

layout:
  - directories: ["*"]
    path: "{directory}/{year}/{identifier}.md"
